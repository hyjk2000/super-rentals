import MapUtil from 'super-rentals/utils/google-maps';
import { module, test } from 'qunit';

module('Unit | Utility | google maps');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = MapUtil.create();
  assert.ok(result);
});
